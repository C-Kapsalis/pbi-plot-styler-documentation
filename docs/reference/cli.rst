CLI reference
=============

.. Port from pbi-plot-styler-docs/reference/cli.md. Per the CLI archetype notes: the single command's flags, the plotstyler.toml key each flag mirrors (precedence: CLI flags > config file > defaults), and the three exit codes (0 clean, 1 drift, 2 error).
