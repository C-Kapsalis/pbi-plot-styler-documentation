Enforce styling in CI
=====================

.. NEW PAGE, no existing draft (this project currently has no how-to guides at all). Cover the exit-code gate from the project README: pbi-plot-styler REPORT --dry-run in a pipeline, exit 1 means a PR added a measure without re-running the styler.
